/**
 * Numberとして表すことができるものよりも大きい最小値との差を表しています。
 */
console.log('Number.EPSILON is %s', Number.EPSILON);

console.log('Number.isInteger(1.0) is %s', Number.isInteger(1.0));
console.log('Number.isInteger(1.1) is %s', Number.isInteger(1.1));
console.log('Number.isInteger(1) is %s', Number.isInteger(1));
console.log('Number.isInteger(false) is %s', Number.isInteger(false));

console.log('Number.isNaN(NaN) is %s', Number.isNaN(NaN));
console.log('Number.isNaN(1) is %s', Number.isNaN(1));
